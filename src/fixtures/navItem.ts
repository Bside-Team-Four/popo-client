import NavItem from '@/types/NavItem';

const fixturesNavItem:NavItem = {
  title: 'PoPo',
  url: '/',
  activeIcon: 'popo-active',
  inactiveIcon: 'popo-inactive',
};

export default fixturesNavItem;
