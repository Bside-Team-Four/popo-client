type ProfileType = {
  userName: string
  schoolName: string,
  grade: number,
  age: number,
  selectedPOPOCount: number,
  selectPOPOCount: number,
  followerCount: number,
  followingCount: number,
  reward: number,
  gender:number,
  // Session에 로그인 한 이메일 저장 방식
  // email: string,
  profileImageUrl: string,
};

export default ProfileType;
